stages:
  - test

Linter:
  stage: test
  script:
    - docker --version
    - docker-compose --version
    - docker run hello-world
    - npm --version
    - apt-get install npm
  only:
    - gitlab-ci
  except:
    - merge_requests

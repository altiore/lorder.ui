import { TimeLineTsx, Y_HEIGHT_BIG } from './time-line';

import { withResize } from '@hooks/withResize';

export const TimeLine = withResize(TimeLineTsx);
export const TIME_LINE_HEIGHT = Y_HEIGHT_BIG;

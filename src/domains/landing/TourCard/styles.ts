import { Theme } from '@material-ui/core';

const IMAGE_SIZE = 350;

export const styles = (theme: Theme): any => ({
  card: {
    width: 350,
  },
  media: {
    height: IMAGE_SIZE,
    width: IMAGE_SIZE,
  },
});

import { Theme } from '@material-ui/core';

export const styles = (theme: Theme): any => ({
  bullet: {
    display: 'inline-block',
    margin: '0 2px',
    transform: 'scale(0.8)',
  },
  card: {
    minWidth: 275,
  },
  pos: {
    marginBottom: 12,
  },
  title: {
    fontSize: 14,
  },
});

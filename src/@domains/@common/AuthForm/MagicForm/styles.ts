import { Theme } from '@material-ui/core/styles';

export const styles = (theme: Theme): any => ({
  form: {
    paddingBottom: 18,
    width: 300,
  },
  wrapper: {
    alignItems: 'center',
    display: 'flex',
    justifyContent: 'center',
    width: 330,
  },
});
